#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export FRONT_AGENT_CMD="$DIR/front-agent"
if ! command -v go >/dev/null 2>&1; then
  echo "Go 1.22 or newer is required to run front-agent" >&2
  exit 127
fi

exec go run "$DIR/../cmd/front-agent" "$@"
