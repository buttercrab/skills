interface:
  display_name: "Front Agent Orchestration"
  short_description: "Gateway agent protocol for humans and main agents"
  default_prompt: "Use $front-agent-orchestration to run a separate human-facing approval gateway and main implementation session through the validated Agent Mail protocol, following the skill's pairing, explicit-approval, lifecycle, and safety rules."
