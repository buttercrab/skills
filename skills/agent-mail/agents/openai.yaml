interface:
  display_name: "Agent Mail"
  short_description: "Rust Postgres mailbox service"
  default_prompt: "Use $agent-mail with the Rust Postgres Agent Mail service: start participants, add project namespaces, send project mail, read participant inboxes, read message bodies, and mark messages read."

policy:
  allow_implicit_invocation: true
