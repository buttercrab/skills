interface:
  display_name: "Align Work"
  short_description: "Align nontrivial coding work before execution"
  default_prompt: "Use $align-work to align on this nontrivial coding task, present one plain-language approval request, then execute and verify within that scope."
policy:
  allow_implicit_invocation: true
