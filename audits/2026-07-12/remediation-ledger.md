# Remediation Ledger

| Scope | Validated finding classes | Remediation | Regression gate |
| --- | --- | --- | --- |
| Agent Mail | Identity impersonation, unbound MCP reads, missing mixed-interface notifications, false readiness, retry duplication, unbounded sessions, live-smoke authority, fixed PostgreSQL path | Participant tokens, session identity checks, shared notifications, `/live` + `/ready`, durable idempotency, session limits/delete, guarded smoke, portable discovery, cursor pagination, enforced rotation-token separation | 5 Rust tests plus real PostgreSQL HTTP/MCP suites |
| Brief Linked Evidence | Secret bypasses, malformed-type crashes, contradictory answer status, unanchored actions, loose locators/provenance, non-strict JSON | Strict parser and schemas, recursive secret checks, typed locators, global IDs, root-ledger semantics, authorization/receipt ordering | 18 tests |
| Execute Goal Loop | Missing goal lifecycle, skipped required gates, generated publish authority, overtriggering, unauthorized reviewers, stagnation | Exact goal tools and blocker rules, required receipts, source-request authority, narrowed triggers, reviewer and progress contracts | Canonical validation plus targeted contract assertions and independent review |
| Front Agent | Metadata/YAML injection, unauthenticated peers, unsafe paths/locks, symlink-following identity state, early acknowledgment, duplicate answers/sends, false smoke, polling overruns, missing focused coverage, stale binary | Participant credentials, typed single-document YAML, private descriptor-based no-follow state with owner/mode/regular-file checks, FD locks, output-first read state, generation-aware idempotency, shared offline backend, cursor/deadline bounds, focused tests, source-only launcher | Vet, unit, race, shuffle, offline subprocess smoke; per-identity symlink, unsafe-mode, and non-regular state regressions |
| Map Technical Landscapes | Unrepresentable accounting/relationships, false grounding/stopping, conflicting identities, duplicate evidence, credential URLs, non-JSON numbers | v2 contract and JSON Schema, exact objects, reconciled accounting, grounded gaps/claims, chronological stop ledger, strict duplicate-key JSON and locators | 33 tests plus schema validation |
| Mine History Tool Landscapes | Arbitrary file reads, publication leaks, metadata smuggling, unsafe keys/symlinks, campaign leakage, linkable IDs, forgeable accounting, ungrounded evidence | v2 contracts, descriptor-bound reads, bounded inputs, secure salted IDs, exact index reconciliation, typed artifacts/lineage, closed publication and redaction, strict JSON at every boundary | 23 synthetic pipeline tests |
| Repository | Missing inventory, cwd-relative resources, production smoke in local validation, partial installer, bytecode staging, trigger/reference drift | Complete README, installed-skill path resolution, live gate separation, build-before-link installer, ignore rules, explicit ownership/reference links | Inventory/link reconciliation and isolated installer tests |
| Align Work | Not audited | Explicitly excluded by the user after late discovery; interrupted audit-only edits were reversed and post-baseline external changes preserved | Baseline/restoration hash ledger only; no validator or audit verdict |

Every validated P0-P2 finding in the six audited skills maps to a patch and a passing local regression gate. External production and real-private-history checks remain outside the authorized scope and are not represented as passes. No claim is made about `align-work` quality.
